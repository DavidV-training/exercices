version: '3.1'

services:
  db:
    image: mariadb:10.5
    environment:
      - MYSQL_USER=mamadou
      - MYSQL_PASSWORD=mamadou
      - MYSQL_ROOT_PASSWORD=mamadou
      - MYSQL_DATABASE=formation
    volumes:
      - ./data:/var/lib/mysql
  front:
    image: adminer
    ports:
      - 8080:8080